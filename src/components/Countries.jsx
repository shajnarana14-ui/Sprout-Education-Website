import { destinations } from '../data/siteData';
import { Icon } from './Icons';
export default function Countries(){return <section className="section countries" id="destinations"><div className="container"><div className="section-heading heading-row"><div><span className="section-kicker">Study destinations</span><h2>Where will your <em>ambition</em> take you?</h2></div><a className="text-link" href="#all-destinations">Explore all destinations <Icon name="arrow"/></a></div><div className="country-grid">{destinations.map(([flag,name,desc],i)=><article className="country-card" key={name}><div className={`country-image country-${i+1}`}><span className="flag">{flag}</span><span className="card-index">0{i+1}</span></div><div className="country-content"><h3>{name}</h3><p>{desc}</p><a href={`#${name.toLowerCase().replaceAll(' ','-')}`}>Explore More <Icon name="arrow" size={17}/></a></div></article>)}</div><a className="mobile-explore btn btn-outline" href="#all-destinations">Explore more</a></div></section>}
